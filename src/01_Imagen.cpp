#include <SFML/Graphics.hpp>

int main()
{
    // Crear una ventana
    sf::RenderWindow window(sf::VideoMode(800, 600), "SFML Image");

    // Cargar la imagen desde un archivo
/* Archived example: 01_Imagen.cpp
   Replaced during cleanup. Main Tetris is in src/tetris.cpp
*/
